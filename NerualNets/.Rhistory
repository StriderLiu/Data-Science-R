install.packages("foreign")
require(foreign)
? read.ssd
? read.ssd()
?read
install.packages("RevoScaleR")
clear()
clear
sayHello <- function()
{}
sayHello <- function()
{
print("Hello, World!")
}
sayHello()
helloPerson <- function(name)
{
sprintf("Hello %s", name);
}
helloPerson("Vincent")
？getClass()
? mode
? str
? typeof
helloPerson <- function(first, last="Jason", ...)
{}
helloPerson <- function(first, last="Jason", ...)
{sprintf("Hello %s %s", first, last)}
helloPerson <- function(first, last="Jason", ...)
{
sprintf("Hello %s %s", first, last);
}
helloPerson("Tom")
helloPerson("Tom", "Lander", "Goodbye")
SqureNum <- function(x)
{
return(x^2);
}
SquareNum <- function(x)
{
return(x^2);
}
SquareNum(5)
SquareNum(13)
RunThis <- function(x, func = mean)
{
do.call(func, args = list(x));
}
RunThis(1:10)
RunThis(1:10, sum)
RunThis(1:10, sd)
UseSwitch <- function(x)
{
swith(x,
"a" = "firtst",
"b" = "second",
"c" = "third",
"other")
}
UseSwitch("d")
UseSwitch("a")
UseSwitch <- function(x)
{
switch(x,
"a" = "firtst",
"b" = "second",
"c" = "third",
"other")
}
UseSwitch("a")
UseSwitch("d")
